import type { BlogDetail } from './data';

type Topic = { slug:string; title:string; excerpt:string; minutes:number; focus:string; risk:string; fixture:string; failure:string; evidence:string; boundary:string };
const topics: Topic[] = [
['outsource-idempotency-retry-window-audit','Review outsourced idempotency retry windows before duplicate side effects','Check repeated requests, key scope, expiry, and stored outcomes without using live payment or customer data.',10,'idempotency keys','one logical action can be applied twice after a retry, timeout, or impatient second click','a synthetic order endpoint with accepted, rejected, timed-out, and concurrent requests','a key crosses accounts, two workers accept one operation, or a transient failure is stored forever','request identity, key scope, stored status, response replay, side-effect count, and expiry','The company owner chooses the business meaning of a duplicate and the retention period for keys.'],
['outsource-record-restoration-permission-gate','Set permission gates for outsourced record-restoration testing','Test deletion visibility, linked records, restore rules, and audit evidence with a controlled data set.',10,'soft-delete restoration','a restored record can return without its relationships, permissions, or expected history','synthetic parent and child records spanning active, deleted, restored, and inaccessible states','deleted data leaks through another query, restoration revives an unsafe link, or an audit entry is missing','record state, query visibility, relationship state, authorization result, audit entry, and cache state','The data owner decides retention, legal holds, permanent deletion, and who may restore a record.'],
['outsource-feature-rule-precedence-audit','Audit outsourced feature-rule precedence with named test personas','Verify defaults, rule precedence, rollout identity, and flag removal using named test personas.',9,'feature-flag targeting','the wrong tenant or user receives unfinished behavior because targeting rules disagree','named synthetic personas that differ by tenant, role, locale, and explicit override','a default differs between services, stale cache preserves access, or rule order selects the wrong variation','persona, evaluated rule, returned variation, application behavior, cache state, and evaluation time','Product and release owners approve audiences, rollout percentage, emergency changes, and flag retirement.'],
['outsource-message-suppression-state-audit','Trace outsourced message suppression states without contacting real recipients','Trace queued, sent, bounced, retried, and suppressed messages while protecting customer addresses and sender authority.',10,'email delivery states','the application reports success while a provider rejects, retries, or suppresses the message','reserved example addresses and a test mailbox covering success, temporary failure, hard failure, and suppression','events arrive out of order, a retry sends twice, a suppression is hidden, or an address appears in logs','message identity, application state, provider event, retry count, visible status, and terminal outcome','The company retains sender configuration, customer communication, reputation policy, and production provider access.'],
['outsource-privileged-action-trace-audit','Reconstruct privileged-action traces with an outsourced programmer','Check actor, action, target, time, and tamper boundaries without exposing sensitive production history.',11,'audit-log integrity','a security or business action cannot be reconstructed because its actor, target, or outcome is ambiguous','synthetic users performing allowed, denied, edited, exported, and administrative actions','an administrator action lacks an actor, denial is omitted, mutable text overwrites history, or clocks disagree','actor identity, action, target, timestamp convention, outcome, source revision, and correlation reference','Security and legal owners decide retention, access, monitoring, and incident response requirements.'],
['outsource-federated-login-return-path-audit','Review federated-login return paths without sharing provider credentials','Exercise state, redirect, denial, expiry, and account-linking failures with a local identity-provider fixture.',11,'OAuth callback errors','a failed authorization flow creates an open redirect, confusing account link, or reusable callback state','a local provider stub with valid, denied, expired, mismatched-state, and repeated callbacks','state is unchecked, an arbitrary redirect survives, denial crashes, or a callback can be replayed','callback parameters, validated state, redirect destination, session result, user message, and safe log record','The security owner controls registered redirects, client secrets, account-linking policy, and production identity settings.'],
['outsource-async-export-revocation-audit','Test asynchronous export revocation across job and download boundaries','Test authorization, snapshot scope, expiry, cancellation, and download behavior using synthetic exports.',10,'background export jobs','an export contains records outside its approved scope or remains downloadable after authority changes','synthetic accounts with distinct roles, date ranges, ownership, cancellation, and expiry states','permissions are checked only at request time, cancellation still publishes, or an expired link remains usable','requesting identity, selected scope, job state, output manifest, download check, and deletion time','The privacy owner approves export fields, retention, notification, and access policy.'],
['outsource-optional-field-contract-audit','Audit optional API field contracts across missing and empty values','Compare absent, null, empty, and default values across server contracts and representative clients.',9,'API nullability','a client treats missing, null, empty, and default values alike when the service does not','contract examples varying one optional field across absent, null, empty, valid, and malformed values','runtime contradicts the schema, a default hides lost data, or an older client crashes on a new null','request shape, response shape, schema declaration, generated type, client result, and error behavior','The API owner decides compatibility, deprecation, default meaning, and supported client versions.'],
['outsource-recurring-job-clock-boundary-audit','Review recurring-job clock boundaries with a controllable scheduler','Test zone selection, daylight changes, missed runs, and duplicate prevention with a controllable clock.',10,'scheduled-task timezones','a job runs twice, is skipped, or uses the wrong business date around a clock change','a controllable scheduler covering midnight, month end, daylight transitions, downtime, and restart','server-local time overrides policy, restart duplicates work, or a nonexistent local time silently shifts','configured zone, intended wall time, UTC instant, execution identity, catch-up choice, and business date','The operations owner chooses the business timezone, catch-up policy, service window, and production schedule.'],
['outsource-browser-resource-policy-audit','Audit browser resource-policy reports before staged enforcement','Classify browser reports, remove noise, and test required resources before an owner approves enforcement.',10,'Content Security Policy reports','a policy blocks required behavior or leaves an unsafe source broadly allowed','a preview page loading approved scripts, styles, images, frames, and deliberately blocked resources','an inline exception becomes universal, reports expose sensitive URLs, or an integration lacks an owner','route, directive, blocked URI category, browser, required behavior, and proposed narrow source','The security owner approves directives, reporting endpoints, exceptions, and enforcement.'],
['outsource-search-result-authorization-audit','Test search-result authorization with contrasting synthetic tenants','Verify indexing, query filters, deletions, and permission changes against isolated synthetic tenants.',11,'search-index permissions','search results expose a record that the requesting identity cannot open in the source system','two synthetic tenants with public, private, transferred, deleted, and permission-changed records','a filter is absent, stale permissions remain searchable, snippets leak text, or deletion misses a replica','source permission, indexed fields, query identity, result, click-through authorization, and removal delay','Security and product owners define searchable fields, tenant isolation, deletion timing, and freshness.'],
['outsource-app-link-authentication-audit','Review app-link authentication across install and sign-in states','Check route ownership, parameters, authentication, fallback, and deferred navigation on controlled devices.',10,'mobile deep links','a link opens the wrong screen, loses its destination, or carries untrusted parameters through login','test links for installed, uninstalled, signed-in, signed-out, expired-session, and malformed-path states','an unverified domain opens the app, login drops context, a fallback loops, or parameters reach logs','input URL, device state, resolved app or browser, authentication step, final screen, and rejected parameters','Product and security owners approve destinations, web fallbacks, association files, and release timing.'],
].map(([slug,title,excerpt,minutes,focus,risk,fixture,failure,evidence,boundary])=>({slug,title,excerpt,minutes,focus,risk,fixture,failure,evidence,boundary} as Topic));

const sources=[{name:'NIST Secure Software Development Framework',url:'https://csrc.nist.gov/Projects/ssdf',note:'First-party practices for reducing software development risk.'},{name:'GitHub pull request review documentation',url:'https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/reviewing-changes-in-pull-requests/about-pull-request-reviews',note:'First-party guidance for review evidence and approval boundaries.'},{name:'Google Technical Writing',url:'https://developers.google.com/tech-writing',note:'Guidance for usable technical instructions.'}];
function detail(t:Topic):BlogDetail{return {strictNoPricing:true,takeaways:[`Define the ${t.focus} decision before granting access.`,`Use ${t.fixture}.`,'Record failed and untested cases beside passes.','Keep production and policy authority with the company owner.'],readinessRows:[{area:'Scope',ready:`One ${t.focus} boundary and named outcome`,notReady:'A request to check the whole system'},{area:'Fixture',ready:t.fixture,notReady:'Live customer records or shared credentials'},{area:'Evidence',ready:t.evidence,notReady:'A pass label without context'},{area:'Authority',ready:t.boundary,notReady:'The programmer makes policy or release decisions'}],sections:[
{id:'name-the-decision',title:`Name the ${t.focus} decision`,paragraphs:[`${t.title} starts with a specific risk: ${t.risk}. Write that risk into the ticket and name the route, worker, client, or data flow under review. The programmer needs an observable question, not a broad request to make the system safer.`,`Record the current behavior, expected behavior, environment, source revision, and decision owner before changing code. ${t.boundary} The outsourced programmer prepares evidence and a focused correction, then hands the decision back.`]},
{id:'build-safe-fixtures',title:'Build fixtures that expose disagreement',paragraphs:[`Use ${t.fixture}. Keep every identity and record synthetic, label the expected state, and change one condition at a time. Contrasting fixtures expose incorrect sharing, fallback, or state transitions that a single successful case hides.`,`Include ordinary use, a denied path, malformed input, a repeat attempt, and a partial failure when they apply. Stop if a useful test would require customer data, an unrestricted credential, or a production action. Record the blocked case instead of quietly widening access.`]},
{id:'trace-the-path',title:'Trace the complete path',paragraphs:[`Follow the input from its first boundary through validation, persistence, asynchronous work, cache or client behavior, and the final visible result. For ${t.focus}, capture ${t.evidence}. This keeps the review tied to behavior rather than one implementation file.`,`Mark each observation as reproduced, inferred, environment-limited, or not tested. A type declaration, successful request, or clean log proves only its own layer. If two layers disagree, preserve the first mismatch and the fixture that revealed it.`]},
{id:'exercise-failure',title:'Exercise failure without creating harm',paragraphs:[`The review should deliberately look for this class of failure: ${t.failure}. Trigger it only in an approved environment with reversible data. Capture the expected response, actual response, durable state, and whether retry or recovery changes the outcome.`,`Do not repair the fixture during the observation and then report an uninterrupted pass. If manual help was required, write down the intervention. That detail often identifies the missing check, instruction, permission, or ownership boundary.`]},
{id:'review-a-narrow-change',title:'Review the smallest defensible change',paragraphs:[`When evidence supports a correction, keep it local to the first proven mismatch. Add a regression case that fails before the change and passes after it. Review adjacent callers only where they share the same contract; do not turn a bounded ${t.focus} ticket into an architecture rewrite.`,`The pull request should explain the fixture, old behavior, changed behavior, checks run, and remaining uncertainty. Link first-party guidance when it informs the method, but do not use a standards link as proof that this application behaves correctly.`]},
{id:'close-the-handoff',title:'Close with a decision-ready handoff',paragraphs:[`Deliver the case matrix, fixture, test output, exact revision, failed cases, exclusions, and questions for the named owner. State the environment so another reviewer can reproduce the result without the author present.`,`A useful outcome can be a verified pass, a contained defect, or a named uncertainty. The outsourced programmer owns accurate implementation evidence. The company retains customer-data, security, policy, merge, deployment, and residual-risk decisions.`]}],roleBrief:[{label:'Assignment',text:`Review one ${t.focus} path with written expected results.`},{label:'Fixture',text:t.fixture},{label:'Evidence',text:t.evidence},{label:'Handoff',text:'Provide the revision, case matrix, results, exclusions, and next owner.'}],faqs:[{question:'Can this review use production data?',answer:'Use synthetic data in an approved environment. Escalate any case that cannot be tested safely without production access.'},{question:'Who approves a behavior change?',answer:'The company technical or policy owner approves behavior, merge, and release decisions.'},{question:'What counts as complete?',answer:'The handoff identifies the revision, fixture, expected and observed results, failed or untested cases, and next owner.'}],sources,internalLinks:[{label:'Plan an outsourced programmer role',url:'/blog/outsourced-programmers-planning',note:'Define scope and ownership.'},{label:'Use a technical handoff',url:'/blog/outsourced-programmer-technical-handoff',note:'Package reproducible evidence.'},{label:'Review access controls',url:'/blog/outsourced-programmer-access-checklist',note:'Limit tools and permissions.'}]};}
export const september3BlogBatch=topics.map(({slug,title,excerpt,minutes})=>({slug,title,excerpt,minutes,published:'2026-09-03'}));
export const september3BlogDetails:Record<string,BlogDetail>=Object.fromEntries(topics.map(t=>[t.slug,detail(t)]));
