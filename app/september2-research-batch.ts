import type {ResearchPost} from './fleet-content';
type Study={slug:string;title:string;excerpt:string;topic:string;question:string;sample:string;method:string;measures:string;failures:string;limits:string};
const studies:Study[]=[
['september-2-outsourced-webhook-authenticity-evidence-study','What evidence proves a webhook was authenticated before processing?','A bounded study of signatures, raw payloads, replay windows, key rotation, and side effects in delegated integration work.','Webhook authenticity evidence','which observations distinguish an authenticated delivery from a well-formed request','a local receiver and sender using test keys across valid, altered, stale, repeated, unknown-key, and rotated-key events','fix the clock and payload bytes, vary one authentication input, trace rejection before business logic, and count durable effects','signature outcome, timestamp decision, key identifier, handler entry, side-effect count, replay result, and safe diagnostics','parsing changes bytes, stale messages pass, unknown keys fall back, or rejected deliveries create effects','test keys and one receiver cannot represent every provider, proxy, algorithm, rotation window, or production threat'],
['september-2-outsourced-cache-freshness-evidence-study','How can teams measure cache freshness after delegated software changes?','Research on mutation-to-read delay, invalidation coverage, permission changes, and evidence limits across cache layers.','Cache freshness evidence','how consistently approved reads reflect a source change across known cache paths','synthetic records changed by create, update, permission reduction, delete, and rollback while browser, edge, application, and query caches are observed','record the source revision, probe each path at fixed intervals, identify the first fresh read, and repeat with cold and warm states','commit time, cache key or tag, layer, age, returned revision, invalidation event, first-fresh time, and stale reads','one layer remains stale, permission reductions are cached, negative entries outlive policy, or rollback mixes revisions','a controlled environment cannot establish peak-load, multi-region, provider-outage, eviction, or every client behavior'],
['september-2-outsourced-database-constraint-evidence-study','Which evidence supports a database-constraint change by an outsourced programmer?','A study of existing-data scans, concurrent writes, migration locks, application errors, and rollback boundaries.','Database constraint evidence','whether a constraint enforces its invariant without hiding incompatible records or unsafe rollout behavior','an isolated schema with valid, duplicate, orphaned, null, concurrent, legacy, and rollback fixtures','state the invariant, scan existing shapes, apply the migration, attempt conflicting writes through database and app paths, then rehearse rollback','constraint identity, scan denominator, conflicting rows, lock duration, write result, visible error, migration revision, and rollback','validation races, rows block migration, app and database rules differ, or rollback cannot restore service','fixture scale cannot predict production locks, replication lag, every legacy shape, deployment ordering, or cleanup decisions'],
['september-2-outsourced-session-revocation-evidence-study','How should outsourced teams test session revocation across application paths?','Research on logout, credential change, role removal, concurrent devices, caches, and background authorization.','Session revocation evidence','when an authority-changing event becomes effective across browser, API, realtime, and queued work','synthetic identities on two devices with logout, password reset, role reduction, disablement, idle expiry, and absolute expiry','timestamp the source event, repeat the same allowed action across each path, record first denial, and distinguish expiry from active revocation','event time, credential issue time, claims, server check, cache age, action result, first-denied time, and remaining paths','one device stays active, queued work uses stale authority, realtime persists, or renewal restores removed access','test identities cannot establish every provider guarantee, offline client, region, clock skew, outage, or incident requirement'],
['september-2-outsourced-batch-recovery-evidence-study','What makes partial batch recovery evidence credible?','A bounded study of item identity, mixed outcomes, retries, compensation, reporting, and operator authority.','Batch recovery evidence','whether failed items can be recovered without repeating completed effects or concealing loss','synthetic batches mixing valid, invalid, duplicate, slow, dependent, and interrupted items in different orders','assign stable batch and item identities, interrupt at controlled points, reconcile accepted work with durable effects, then apply documented recovery','input, accepted and rejected counts, item state, side-effect identity, retries, summary status, and reconciliation difference','success hides failures, retry repeats completed items, order changes meaning, or compensation creates another mismatch','small fixtures do not model every workload, outage, operator mistake, retention rule, or atomicity decision'],
].map(([slug,title,excerpt,topic,question,sample,method,measures,failures,limits])=>({slug,title,excerpt,topic,question,sample,method,measures,failures,limits} as Study));
const sources:Array<[string,string]>=[['NIST Secure Software Development Framework','https://csrc.nist.gov/Projects/ssdf'],['CISA Secure by Design','https://www.cisa.gov/securebydesign'],['OWASP Application Security Verification Standard','https://owasp.org/www-project-application-security-verification-standard/'],['DORA research program','https://dora.dev/research/'],['OpenTelemetry documentation','https://opentelemetry.io/docs/'],['GitHub pull request reviews','https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/reviewing-changes-in-pull-requests/about-pull-request-reviews']];
function body(s:Study){return ['September 2 research scope: this is a newly dated evidence record for the named topic.',`Research question: ${s.question}? This study treats the answer as a reproducible system observation, not a judgment about a programmer or ticket volume.`,`The sample is ${s.sample}. Record environment, revision, fixture version, clock basis, reviewer, and exclusions before collection so later readers know the boundary.`,`Methodology: ${s.method}. Keep expected and observed results separate, and preserve manual intervention or fixture changes beside each case.`,`Primary measures are ${s.measures}. Each needs a collection point and time; a value without provenance cannot identify which layer produced it.`,`Negative observations include ${s.failures}. Exercise them with synthetic data and task-limited access. Stop at production credentials, customer records, live releases, or policy exceptions.`,'NIST SSDF and CISA Secure by Design support intentional verification and ownership. OWASP ASVS supplies testable security questions. These sources shape the method but do not certify an application.','DORA research treats reliability as a property of the system of work. GitHub review documentation provides a durable revision and approval record. OpenTelemetry can connect approved observations across service boundaries.',`Analysis must publish denominator, failures, exclusions, and uncertainty. ${s.limits}. A clean result supports only the named sample, revision, and environment.`,'An outsourced programmer can build fixtures, run approved checks, propose a narrow correction, and package a reproducible handoff. Company owners retain production access, secrets, data, policy, architecture, merge, release, and risk authority.',`Conclusion: credible ${s.topic.toLowerCase()} links a bounded question to controlled cases, direct measures, negative observations, and explicit limits. The next task addresses the first unresolved observation and names its authorized owner.`]}
export const september2ResearchBatch:ReadonlyArray<ResearchPost>=studies.map(s=>({slug:s.slug,route:`/research/${s.slug}`,datePublished:'2026-09-02',campaignDate:'2026-09-02',sourceDateField:'sourceDate',sourceDate:'2026-09-02',title:s.title + ' — September 2 research note',excerpt:s.excerpt,topic:s.topic,published:'2026-09-02',body:body(s),sources}));
